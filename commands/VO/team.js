class Team {
  /**
   *
   * @param {Result} result
   * @param {Side} side
   * @param {Player[]} players
   */
  constructor(result, side, players) {
    this.result = result;
    this.side = side;
    this.players = players;
  }
}
