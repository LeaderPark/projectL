class Kda {
  /**
   *
   * @param {int} kills
   * @param {int} deaths
   * @param {int} assist
   */
  constructor(kills, deaths, assist) {
    this.kills = kills;
    this.deaths = deaths;
    this.assist = assist;
  }
}

module.exports = Kda;
